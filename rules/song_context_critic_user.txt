Evaluate this song context against parsed song facts and technical statuses.

PARSED SONG FACTS:
{{PARSED_SONG_JSON}}

SONG CONTEXT DRAFT:
{{SONG_CONTEXT_JSON}}

PREVIOUS FAILED RESULT / REPAIR CONTEXT:
{{PREVIOUS_RESULT_JSON}}

PARSER TECHNICAL STATUS:
{{PARSER_STATUS_JSON}}

WRITER TECHNICAL STATUS:
{{WRITER_STATUS_JSON}}

FAILED STAGE IF ANY:
{{FAILED_STAGE}}

Return ONLY valid JSON with exactly these keys:
{
  "success": false,
  "score": 0,
  "issues": [],
  "repairs": []
}

Compact output limits:
- Max 6 issues and max 6 repairs.
- Each issue/repair max 18 words.
- Do not return the song context, repaired context, parsed facts, writer result, analysis, or sub-scores.

Critic checks:
- Fail if parser/writer technical status reports invalid JSON or unusable structure.
- Fail if the context invents unsupported lore, style, setting, species, props, factions, characters, or visual technology.
- Fail if continuity_rules contain concrete characters, factions, landmarks, props, symbols, weapons, rituals, destinations, or central composition requirements not required in every clip.
- Fail if inventory fields are wordy, duplicated, or treated as mandatory scene content.
- Fail if avoid contains desired motifs instead of source-grounded forbidden content/style/drift.
- Success requires compact reusable continuity that preserves identity without overriding future lyrics.
