Create visual generation prompts for one short music-video timeline block.

PRIORITY 0 — VISUAL STYLE, mandatory:
{{VIDEO_STYLE}}

PRIORITY 1 — GLOBAL SONG CONTEXT, low priority:
Use this only for recurring world, characters, tone, motifs and visual continuity.
Do not cram every global motif into the current shot.
{{GLOBAL_CONTEXT_JSON}}

PRIORITY 2 — LOCAL CONTEXT, medium priority:
Use this only for continuity, transition logic and neighboring scene awareness.
Do not let local context override the current block.
{{LOCAL_CONTEXT}}

PRIORITY 3 — CURRENT BLOCK, highest priority:
This is the block you must depict now.
Block index: {{BLOCK_INDEX}}
Block kind: {{BLOCK_KIND}}

{{CURRENT_BLOCK}}

PREVIOUS GENERATED VISUAL SUMMARIES:
Use only for continuity of established visuals.
{{CONTINUITY}}

{{LITERAL_SCENE_RULES}}

Priority rules:
- If global context, local context, continuity, and current block conflict, follow the current block.
- CURRENT_BLOCK may describe a semantic range and an internal subrange. If it contains CURRENT SUBRANGE, that subrange is the highest factual priority for what happens now.
- FULL SEMANTIC RANGE LYRICS / RANGE TEXT is context for meaning and continuity; do not make earlier/later lyrics the main action when a CURRENT SUBRANGE slice is provided.
- For verse blocks, depict the current semantic range/subrange mainly, not the whole song.
- Global context may influence style, world, recurring design, and optional background details only.
- Local context may influence transitions and continuity only.
- Do not copy raw lyrics into image_prompt or video_prompt.
- Do not ask the image/video model to render visible text.
- Choose only a few relevant props for the block. Do not include every recurring prop.

Action-progression rules for video_prompt:
- Infer a concrete visual micro-event from the current block even when the lyrics are metaphorical, repetitive, chanted, or abstract.
- The video_prompt must cover the whole generated duration as a single continuous non-looping event: beginning, development, and changed end state.
- Write it with explicit temporal phrases: "At the start... Then... By the end...".
- Include at least one purposeful character action, one object/environment reaction, and one visible consequence by the end.
- The final frame must be visibly changed from the starting frame; do not return to the same pose, same composition, or same state.
- Do not make camera movement the only motion. Camera movement may support the action, but characters, objects, lighting, props, or the environment must actually change.
- Avoid idle-only animation: no loop-like swaying, no repeated rhythmic gestures, no endless floating particles, no static pose with only smoke/light/hair movement.
- Prefer irreversible physical verbs and cause/effect: raises, grabs, turns, opens, shatters, scatters, extinguishes, collapses, reveals, transforms, strikes, recoils, retreats, bursts, falls, floods, consumes, clears.
- For repeated chorus/refrain blocks, escalate the event or show a later consequence instead of repeating the same performance pose.
- Keep the action consistent with the still image and the current block; do not invent unrelated plot twists.

Task:
Return ONLY a valid JSON object with exactly these keys:
{
  "scene_summary": "one sentence summary of the literal current-block visual event",
  "image_prompt": "prompt for still starting image, in English, no raw lyrics, no visible text",
  "video_prompt": "prompt for animating the image as a complete visible action arc over the full clip duration, in English, no raw lyrics, no visible text",
  "negative_prompt": "comma-separated things to avoid, including idle animation and loop-like motion"
}

Rules:
- image_prompt and video_prompt must be in English.
- Preserve the supplied visual style.
- Do not include subtitles, captions, signs, banners, calligraphy, written words, letters, logos, or watermarks in image/video prompts.
- negative_prompt must include: text, letters, captions, subtitles, signs, calligraphy, logo, watermark, manuscript page, poster layout, idle animation, looping motion, camera-only movement.

Additional verse-block rules:
- The main visible action must be derived from CURRENT BLOCK, not from global song context or neighboring verses.
- If CURRENT SUBRANGE is present, make its local meaning the main action beat; use the full semantic range only for context.
- For repeated chorus/refrain blocks, do not output the same idle performance shot again; escalate the visual consequence or change the event stage while preserving continuity.
