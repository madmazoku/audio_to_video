Evaluate this condensed style/identity contract against RAW VIDEO STYLE and PARSED SOURCE FACTS.

Style source id: {{STYLE_SOURCE_ID}}
Range index if range-specific: {{BLOCK_INDEX}}

RAW VIDEO STYLE:
{{RAW_VIDEO_STYLE}}

PARSED SOURCE FACTS:
{{PARSED_STYLE_JSON}}

CONTRACT DRAFT:
{{CONTRACT_JSON}}

PREVIOUS FAILED RESULT / REPAIR CONTEXT:
{{PREVIOUS_RESULT_JSON}}

PARSER TECHNICAL STATUS:
{{PARSER_STATUS_JSON}}

WRITER TECHNICAL STATUS:
{{WRITER_STATUS_JSON}}

FAILED STAGE IF ANY:
{{FAILED_STAGE}}

Return ONLY valid JSON with exactly these keys:
{
  "success": false,
  "score": 0,
  "issues": [],
  "repairs": []
}

Compact output limits:
- Max 6 issues and max 6 repairs.
- Each issue/repair max 16 words.
- Do not return the contract, repaired contract, parser facts, writer result, analysis, or sub-scores.

Critic checks:
- Fail if parser/writer technical status reports invalid JSON or unusable structure.
- Fail if any field invents unsupported medium, genre, era, world, species, technology, setting, palette, character, or plot.
- Fail if style_must_not contains a source-positive required medium, technique, style, desired subject, or desired motif.
- Fail if style_must_not includes invented project-default negatives rather than source-grounded negatives.
- Fail if required_style_prefix omits the source-required medium/look.
- Fail if required_recurring_anchors or style_continuity_rules contain concrete characters, factions, landmarks, props, weapons, symbols, rituals, destinations, or plot beats not explicitly required in every clip.
- Fail if concrete motifs are promoted from optional inventory into repeated requirements.
- Fail if anchor_usage_rule does not preserve current-lyrics primacy.
- Success requires a compact, source-grounded, reusable contract that preserves identity without forcing the same subject/setting/action everywhere.
